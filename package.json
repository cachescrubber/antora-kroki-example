{
  "name": "antora-kroki-example",
  "version": "1.0.0",
  "private": true,
  "description": "Antora demo for resolving Antora example$ resource ids in Kroki PlantUML macros and includes (asciidoctor-intellij-plugin #516)",
  "scripts": {
    "build": "antora --fetch antora-playbook.yml",
    "build:docker": "docker build -t antora-kroki-example . && docker run --rm -v \"$PWD\":/antora -w /antora antora-kroki-example --fetch antora-playbook.yml",
    "test": "node --test extensions/test"
  },
  "devDependencies": {
    "@antora/cli": "3.2.x",
    "@antora/pdf-extension": "1.0.x",
    "@antora/site-generator": "3.2.x",
    "asciidoctor-kroki": "0.18.x"
  }
}
