site:
  title: Kroki example$ demo
  start_page: kroki-antora-example::index.adoc

antora:
  extensions:
    - '@antora/pdf-extension'
    # Inline example$ resource ids in Kroki PlantUML diagrams for the PDF export (see README)
    - ./extensions/antora-assembler-kroki.js

content:
  sources:
    - url: .
      branches: HEAD
      start_path: docs

ui:
  bundle:
    url: https://gitlab.com/antora/antora-ui-default/-/jobs/artifacts/HEAD/raw/build/ui-bundle.zip?job=bundle-stable
    snapshot: true

asciidoc:
  extensions:
    - asciidoctor-kroki
  attributes:
    kroki-server-url: https://kroki.io
    kroki-fetch-diagram: true

output:
  dir: ./build/site
