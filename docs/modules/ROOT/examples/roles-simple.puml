@startuml
' Pulls only the "Core" sub-block out of roles.puml. The feature resolves the Antora
' resource id and extracts the !startsub Core … !endsub section (which itself includes
' the shared layout — nested includes are inlined recursively).
title Party — core only
!includesub example$roles.puml!Core
@enduml
