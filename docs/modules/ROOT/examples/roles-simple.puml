@startuml
' Pulls only the "Core" sub-block out of roles.puml (same directory): the !startsub Core …
' !endsub section, which itself includes the shared layout — nested includes are inlined
' recursively.
title Party — core only
!includesub ./roles.puml!Core
@enduml
